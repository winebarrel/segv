# Segv

Cause SEGV.

## Installation

Add this line to your application's Gemfile:

```ruby
gem 'segv'
```

And then execute:

    $ bundle

Or install it yourself as:

    $ gem install segv

## Usage

```ruby
require 'segv'

Segv.segv
```
